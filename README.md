# COMPLETED - MyReads Project
- Download the project on your machine.
- Under project directory type: `npm install` or `yarn install`.
- After install, type: `npm start` or `yarn start`.
- The project should work as expected.